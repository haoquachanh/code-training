// get element at index 0
// Cú pháp: _.head(array)
// Gets the first element of array.
// Lấy phần tử đầu tiên của mảng.

const _ = require("lodash");

const array = [1, 2, 3];
console.log(_.head(array)); // 1
console.log(_.first(array)); // 1
